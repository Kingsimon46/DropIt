using UnityEngine;
using System.Collections;

public class PlatformController : MonoBehaviour {

	// Use this for initialization
	void Start () {
	    // Setup Platform and load objects with the "ObjectPooler"-Class


	}
	
    // Prepare for new drop
    void StartNewCicle ()
    {
        // Check for correct drop position
        // Call ObjectPooler to destroy current drop and setup platform for new cicle
        // Call ObjectSpawner to spawn new drop


    }
}
